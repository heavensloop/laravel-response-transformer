<?php

namespace {Namespace};

use Heavensloop\DataTransformer\AbstractTransformer as Transformer;

class {ClassName} extends Transformer
{
    protected $name = "transformer-name";

    // protected $options = [];

    /**
     * Transform the data
     *
     * @param Model|Builder|Collection $dataSource
     * @return Model|Builder|Collection $dataSource
     */
    public function transform($dataSource)
    {

    }
}
